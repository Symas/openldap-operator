---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: ldapservers.openldap.gpu-ninja.com
spec:
  group: openldap.gpu-ninja.com
  names:
    kind: LDAPServer
    listKind: LDAPServerList
    plural: ldapservers
    singular: ldapserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LDAPServer is an OpenLDAP server.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: LDAPServerSpec defines the desired state of LDAPServer.
            properties:
              addressOverride:
                description: AddressOverride is an optional address that will be used
                  to access the LDAP server
                type: string
              adminPasswordSecretRef:
                description: AdminPasswordSecretRef is a reference to a secret that
                  contains the password for the admin user.
                properties:
                  name:
                    description: Name is the name of the secret.
                    type: string
                required:
                - name
                type: object
              certificateSecretRef:
                description: CertificateSecretRef is a reference to a secret that
                  contains the TLS certificate and key that will be used to secure
                  the LDAP server. Expected to be in cert-manager format, eg. tls.key,
                  tls.crt, ca.crt.
                properties:
                  name:
                    description: Name is the name of the secret.
                    type: string
                required:
                - name
                type: object
              debugLevel:
                description: DebugLevel controls the verbosity of the server logs.
                type: integer
              domain:
                description: Domain is the domain of the organization that owns the
                  LDAP server.
                type: string
              fileDescriptorLimit:
                description: 'FileDescriptorLimit controls the maximum number of file
                  descriptors that the LDAP server can open. See: https://github.com/docker/docker/issues/8231'
                type: integer
              image:
                description: Image is the container image that will be used to run
                  the LDAP server.
                type: string
              organization:
                description: Organization is the name of the organization that owns
                  the LDAP server.
                type: string
              storage:
                description: Storage defines the persistent volume that will be used
                  to store the LDAP database.
                properties:
                  size:
                    description: Size is the size of the persistent volume that will
                      be used to store the LDAP database.
                    type: string
                  storageClassName:
                    description: StorageClassName is the name of the storage class
                      that will be used to provision the persistent volume.
                    type: string
                required:
                - size
                type: object
            required:
            - adminPasswordSecretRef
            - certificateSecretRef
            - domain
            - image
            - organization
            - storage
            type: object
          status:
            description: LDAPServerStatus defines the observed state of the LDAPServer.
            properties:
              conditions:
                description: Conditions represents the latest available observations
                  of an LDAPServer's current state.
                items:
                  description: LDAPServerCondition contains details for the current
                    condition of the LDAPServer.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the last transition.
                      type: string
                    reason:
                      description: Reason is a unique, one-word, CamelCase reason
                        for the condition's last transition.
                      type: string
                    status:
                      description: Status is the status of the condition.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  for this LDAPServer by the controller.
                format: int64
                type: integer
              phase:
                description: Phase is the current state of the LDAP server.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
